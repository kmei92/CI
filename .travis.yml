language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script: pytest --doctest-modules pytest_example.py docstring_test.py unittest_example.py